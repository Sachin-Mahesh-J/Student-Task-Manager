{{--
<div class="navbar bg-base-100 shadow border-b border-base-300 px-6 sticky top-0 z-50">
    <div class="flex items-center gap-3">
        <a href="#" class="btn btn-ghost text-xl font-semibold tracking-wide">
            🎓 Student Task Manager
        </a>
    </div>

    <div class="ms-auto">
        <select
            onchange="document.documentElement.setAttribute('data-theme', this.value)"
            class="select select-sm select-bordered"
        >
            <option value="nord">Nord</option>
            <option value="abyss">Abyss</option>
        </select>
    </div>
</div>
--}}
{{-- Header --}}
{{-- Header --}}
<header class="bg-blue-300 text-white py-5 px-6 relative flex items-center">
    <!-- Centered Title -->
    <div class="absolute inset-0 flex justify-center items-center pointer-events-none">
        <h1 class="text-2xl font-bold">🎓 Student Task Manager</h1>
    </div>

    <!-- Invisible spacer to balance layout -->
    <div class="w-[130px]"></div>
</header>

